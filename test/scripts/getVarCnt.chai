global cnt = 0;

def browseRecursive(id) 
{
  auto results = browse(id);
   for(r : results)
   {
      //print(r.Name().Name())
      browseRecursive(r.Id())
      cnt = cnt+1;

   }
}


browseRecursive(NodeId(0,85))
print(cnt);

